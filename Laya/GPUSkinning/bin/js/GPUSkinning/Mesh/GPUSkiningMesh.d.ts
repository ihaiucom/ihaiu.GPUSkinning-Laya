export default class GPUSkiningMesh extends Laya.Mesh {
    static _parse(data: any, propertyParams?: any, constructParams?: any[]): GPUSkiningMesh;
    static LoadAsync(path: string): Promise<GPUSkiningMesh>;
}
